# Model Manifest

This directory stores generated manifest files for trained models.

Expected outputs from `optimization/scripts/destillation/train_student.py`:

- `<model-name>.json`: per-model manifest
- `models.json`: manifest registry
